Fix:
List View now filters names properly.
The main window redraw problem.

Changes:
EditButton now shows the "button" text in the edit control selected when clicked
Clicking on "OK" will copy the text from the edit control into the button label.

Additions:
List view now sorts by last, then first names.
- getHoverButtonText 
